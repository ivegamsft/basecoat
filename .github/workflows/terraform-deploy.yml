name: Terraform Deploy

on:
  push:
    branches: [main]
    paths:
      - 'infra/aws/**'
      - '.github/workflows/terraform-deploy.yml'
  pull_request:
    branches: [main]
    paths:
      - 'infra/aws/**'

env:
  AWS_REGION: us-east-1
  TF_VERSION: 1.5.0

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    strategy:
      matrix:
        working-directory:
          - infra/aws
          - infra/aws/modules/networking
          - infra/aws/modules/database
          - infra/aws/modules/compute
          - infra/aws/modules/caching
          - infra/aws/modules/storage
          - infra/aws/modules/secrets
          - infra/aws/modules/security
          - infra/aws/modules/monitoring

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        working-directory: ${{ matrix.working-directory }}
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        working-directory: ${{ matrix.working-directory }}
        run: terraform validate

  plan-dev:
    name: Plan Dev Deployment
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        working-directory: infra/aws
        run: terraform init -backend=false

      - name: Terraform Validate
        working-directory: infra/aws
        run: terraform validate

  plan-prod:
    name: Plan Production Deployment
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        working-directory: infra/aws
        run: terraform init -backend=false

      - name: Terraform Plan (Dry Run)
        working-directory: infra/aws
        run: terraform plan -var-file=environments/prod/terraform.tfvars -input=false -no-color
        env:
          TF_VAR_aws_primary_region: us-east-1
          TF_VAR_aws_secondary_region: us-west-2
