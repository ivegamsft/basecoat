{
  "version": 1,
  "contracts": [
    {
      "workflow": "extension-deploy.yml",
      "job": "build-push",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    },
    {
      "workflow": "extension-deploy.yml",
      "job": "deploy",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    },
    {
      "workflow": "mcp-build.yml",
      "job": "build",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    },
    {
      "workflow": "mcp-deploy.yml",
      "job": "build-push",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    },
    {
      "workflow": "mcp-deploy.yml",
      "job": "deploy",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    },
    {
      "workflow": "portal-deploy.yml",
      "job": "deploy",
      "allowed_runner_classes": [
        "configurable-deploy",
        "self-hosted-linux"
      ],
      "max_timeout_minutes": 20,
      "required_job_markers": [
        "Validate self-hosted runner routing contract"
      ]
    }
  ]
}
