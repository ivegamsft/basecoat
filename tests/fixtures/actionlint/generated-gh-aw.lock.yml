name: Generated gh-aw compatibility regression

on:
  workflow_dispatch:

permissions:
  copilot-requests: write

jobs:
  generated:
    runs-on: ubuntu-latest
    concurrency:
      group: generated-gh-aw
      queue: max
    steps:
      - run: echo "generated compatibility invocation accepts current gh-aw syntax"
