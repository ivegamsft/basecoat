name: Public OIDC deploy fixture

on:
  workflow_dispatch:

permissions: write-all

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: staging
    steps:
      - name: Authenticate to a public cloud endpoint
        uses: azure/login@example
        with:
          client-id: example
          tenant-id: example
          subscription-id: example
