name: Commented OIDC permission fixture

on:
  workflow_dispatch:

permissions:
  # id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: azure/login@v2
        with:
          client-id: example-client
          tenant-id: example-tenant
          subscription-id: example-subscription
