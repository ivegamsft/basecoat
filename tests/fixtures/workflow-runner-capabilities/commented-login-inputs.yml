name: Commented Azure Login inputs

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: azure/login@example
        with:
          creds: # disabled
          client-id: # disabled
          tenant-id: # disabled
          subscription-id: # disabled
